import type { NextPage } from 'next';
import { Layout } from '@/layouts';

const Home: NextPage = () => {
	return (
		<Layout>
			<h1 className="text-red-500">home</h1>
		</Layout>
	);
};

export default Home;
