import type { NextPage } from "next";

const Home: NextPage = () => {
	return (
		<div className="bg-red-700 h-screen flex items-center justify-center">
			<h1 className="text-red-100 font-bold text-2xl">Alpost</h1>
		</div>
	);
};

export default Home;
