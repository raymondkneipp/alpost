import type { NextPage } from 'next';
import { Layout } from '@/layouts/app';

const Home: NextPage = () => {
	return (
		<Layout>
			<h1 className="text-red-500">app</h1>
		</Layout>
	);
};

export default Home;
