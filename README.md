# Alpost

## Setup

- Set environment variables (.env.example)

- Connect to PlanetScale database

  ```bash
  pscale connect alpost main --port 3309
  ```

- Open prisma studio

  ```bash
  npx prisma studio
  ```

- Run the project
  ```bash
  npm run dev
  ```
